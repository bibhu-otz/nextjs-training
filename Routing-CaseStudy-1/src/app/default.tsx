// ============================================================
// ROOT DEFAULT.TSX
// 
// Required alongside the root layout when using parallel routes.
// This provides a default fallback for the root segment,
// ensuring the routing works correctly during hard navigations.
// ============================================================

export default function Default() {
  return null;
}
